# 架构整理验收状态

**状态：** 批次 1–6 已在隔离分支完成基础落地  
**对照：** `docs/superpowers/specs/2026-07-15-file-architecture-reorganization-design.md`

## 完成项

1. 运行产物治理与文档分层权威关系已建立。
2. 后端 `api` / `domains` / `ai` / `platform` / `shared` 壳已落地，旧路径薄兼容。
3. 需求域拆分为文档 / 解析 / 问题工作流 / 生成上下文 Module；`/api/ai/documents*` 为弃用代理。
4. 执行链路固定为 Router -> ExecutionService -> Harness -> Runner；报告域增加证据/导出/保留策略入口。
5. 前端按 PRD 11 个一级模块补齐导航与模块骨架，共享 Evidence/Timeline/Log/Polling。
6. 预研能力退出默认装配；兼容层清单与删除条件已归档。

## 未在本轮完成（明确非目标或后续）

- 全面删除所有 Python 兼容 re-export（仍有测试/服务内部调用）
- PRD 未实现业务能力的补齐
- 生产级队列、对象存储、完整鉴权
