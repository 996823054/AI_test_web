# 批次 1：仓库卫生与文档权威关系验收证据

## 提交范围

- 实施基线：`a99fc367f05a558dd0d79a595cccdfb9147e864e`（`docs: define platform architecture reorganization`）
- 批次 1 提交：`4e2a130d8f6de1a3ffa933b754bd1404712b7192`（`chore: establish platform repository governance`）
- 本证据生成时间：2026-07-15（UTC+8）
- 补充提交：本文件随审查补充提交创建；最终 SHA 以 Git 提交记录为准，避免在提交内容中写入无法自证的自身 SHA。

## 验证结果

| 验证项 | 命令 | 实际结果 | 证据来源 |
|---|---|---|---|
| 仓库治理 | `python -m unittest tests.test_repository_governance -v` | 通过；3 项测试全部 `ok`，包括文档链接、禁止跟踪运行产物和忽略规则样例。 | 2026-07-15 补充审查复跑 |
| 文档链接 | `python scripts/check_document_links.py` | 通过；检查 38 个本地链接，未发现无效链接。 | 2026-07-15 补充审查复跑 |
| 后端测试 | `cd backend && python -m unittest discover -s tests` | 返回码 0，测试套件 `OK`。 | 批次 1 提交前完整验证 |
| 前端测试 | `cd frontend && npm test` | 返回码 0，Vitest 测试通过。 | 批次 1 提交前完整验证 |
| 前端构建 | `cd frontend && npm run build` | 返回码 0，Vite 生产构建成功。 | 批次 1 提交前完整验证 |

后端和前端结果引用批次 1 提交前已执行的完整验证，本次文档审查未修改业务代码，因此未伪造新的执行时间或测试计数。补充提交前再次执行治理与链接轻量检查，并以最终命令输出为准。

## 验收结论

- 文档链接检查既可单独运行，也由仓库治理测试统一调用。
- 运行产物禁止跟踪和忽略规则均由自动化测试覆盖。
- 产品需求事实源仍为 `docs/product/AI测试平台PRD.md`；模块确认文档只保留索引与边界结论。
- RAG ADR 分离 PRD 产品目标与批次 1 / 当前代码实施范围，阶段性实现不缩窄产品要求。
