# Vision automation (experimental)

`app.api.vision_automation` and `backend/vision_quick_start.py` are pre-research
surfaces. They are not registered in default `register_routers`.

Enable only after formal mobile/report evidence seams are ready.
